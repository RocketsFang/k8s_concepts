apiVersion: v1
kind: Pod
metadata:
  name: demo-pod
spec:
  containers:
  - image: https://cloud.docker.com/repository/docker/rocketsfang/k8s_concepts
    name: pods
    imagePullPolicy: Always
  imagePullSecrets:
  - name: docker-io-account
    